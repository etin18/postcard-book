# 明信片通訊錄 — 需求規格

出國寄明信片時，不用再到處翻大家的地址。打個名字就查到地址，寄完順手記一筆，
回家還能看到這些年寄過哪些明信片給誰。

- **前端**：純 HTML / CSS / JS，無框架、無建置步驟（同 expense-app）
- **後端**：Google Apps Script
- **資料**：存在自己的 Google 試算表
- **照片**：壓縮後只存手機本機（IndexedDB），不上傳
- **離線可用**：地址整份快取，沒網路照樣查；紀錄先存本機，有網路自動補同步

---

## 一、資料結構（Google 試算表兩張表）

### 「朋友」表

| 欄位 | 說明 |
|---|---|
| 編號 | 系統產生，同一人搬家後多列共用同一個編號 |
| 名稱 | 中文名或暱稱，搜尋主要用這欄 |
| 收件人 | 明信片上實際要寫的名字（通常是英文） |
| 地址 | 街道地址 |
| 城市 | |
| 郵遞區號 | |
| 國家 | |
| 狀態 | `現用` / `已搬家` |
| 建檔日 | |

**搬家歷史的作法**：同一個編號可以有多列，但只有一列是`現用`。
按「這位朋友搬家了」時，舊列改成`已搬家`，新增一列`現用`。
搜尋只回傳`現用`那筆，朋友詳情裡才看得到舊地址。

### 「明信片」表

| 欄位 | 說明 |
|---|---|
| 編號 | |
| 朋友編號 | 對應「朋友」表 |
| 朋友名稱 | 寫入當下的名稱快照，改名或刪除朋友時紀錄不會變空白 |
| 寄出國家 | |
| 寄出日期 | |
| 備註 | 寫了什麼內容、當下的心情 |
| 已收到 | 空白 / `是` |
| 照片編號 | 對應手機本機 IndexedDB 裡的照片，試算表不存圖 |
| 建立時間 | |

---

## 二、兩個分頁

設定（試算表連線、通關密語、照片空間、CSV 匯出）不佔分頁，收在右上角齒輪裡。

### 分頁 1：我要寄信

這一頁就是站在郵局櫃檯時的完整動線：**打名字 → 看到地址 → 抄上去 → 往下順手記一筆**。
查地址和記錄不分家，一頁到底。

**上半：查地址**

- 最上面一個搜尋框，**打字即時篩選**，中文名和英文收件人名都能比對得到
- 結果是**簡單列表**，一列一個人：名稱、城市＋國家、右邊一個複製圖示
  （複製會拿到收件人＋地址＋城市郵遞區號＋國家的完整多行格式）
- 點一列展開，顯示完整地址 —— 這就是照著抄的那一刻
- 搜尋不到人時，列表位置直接出現「＋ 新增這個朋友」
- 列右側可進入朋友詳情：編輯、標記搬家、刪除、看搬家歷史、
  看「寄給他的所有明信片」

**下半：記一筆**

展開朋友後，下面直接接著寄送欄位，填完按一下就存好：

- **寄給誰** — 上面選的那位，自動帶入
- **寄出國家** — 自由輸入，但會記住用過的國家做成快選（同 expense-app 的付款人作法）
- **寄出日期** — 預設今天
- **備註** — 選填
- **照片** — 拍照或選圖，壓縮後存本機

一次記一張。存完搜尋框清空，可以直接查下一個人。

### 分頁 2：寄信歷史

**一份列表 ＋ 一排篩選器**，不做多視圖切換。

列表預設依日期新到舊，卡片式，有照片顯示縮圖，點縮圖看大圖。
卡片上可直接勾「已收到」，也能點進去編輯或刪除。

篩選器（可疊加）：

- **朋友** — 選一個人，就看到寄給他的全部，等於「我寄過幾張給他」
- **國家**
- **日期區間** — 要回顧某一趟旅行就拉那段日期，跨國的旅行照樣一次看完
- **只看未收到** — 一個開關，挑出還沒送達的

篩選結果上方顯示「共 X 張」。

---

## 三、照片

沿用 expense-app 的機制：

- 拍照或選圖後先在瀏覽器端壓縮，再存進 IndexedDB
- 試算表只存照片編號，圖片本身不上傳、不佔 Google 空間
- 設定頁可以看照片佔用多少空間
- **代價要講清楚**：清除瀏覽器資料或換手機，照片會不見，
  但文字紀錄在試算表裡是安全的

---

## 四、離線行為

出國時人在國外，可能沒網路或漫遊很貴，所以：

- 通訊錄**整份快取在手機**，開 App 不連網就能查地址（這是最重要的離線情境）
- 新增的明信片紀錄先寫本機，狀態列顯示「待同步」，一有網路自動補送
- 新增／修改朋友同理

---

## 五、沿用 expense-app 的部分

以下直接搬，不重做：

- Apps Script 後端骨架與通關密語（SECRET）驗證機制
- PWA 設定（manifest、Service Worker、加到主畫面）
- 照片壓縮 ＋ IndexedDB 存取
- 離線佇列與自動補同步
- CSV 匯出
- 設定頁的連線設定流程

---

## 六、這一版不做

明確排除，避免日後誤會：

- 朋友自己填地址的分享表單（先由使用者手動輸入）
- 一次勾多人的批次寄出
- 地址標籤列印
- 多人共用同一份資料（單人使用）
- 城市／景點欄位（明信片紀錄只記到國家層級）
- 「旅行」這個概念本身 —— 不自動分組、也不讓使用者命名一趟旅行，
  要回顧就用國家＋日期區間篩
